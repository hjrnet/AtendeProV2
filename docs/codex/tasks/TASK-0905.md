# TASK-0905 — Notificações internas

## Release
R9 — Suporte, central de ajuda, comunicação e refinamentos de experiência

## Complexidade
MÉDIA

## Objetivo
Eventos, avisos e alertas.

## Contexto
Esta task faz parte do plano profissional completo do AtendePro. Deve ser executada de forma controlada pelo Harness Profissional.

## Escopo permitido
- Implementar somente o necessário para Notificações internas.
- Atualizar testes e documentação quando necessário.
- Manter arquitetura definida no AGENTS.md.

## Fora de escopo
- Não implementar tasks futuras.
- Não alterar módulos não relacionados sem justificativa.
- Não fazer push.

## Regras técnicas
- Backend: arquitetura hexagonal quando houver backend.
- Frontend: feature architecture e mobile-first quando houver web.
- Código de negócio em português claro.
- DTOs, Commands, Results, UseCases, Ports e Adapters.
- Testes obrigatórios para regra de negócio.

## Critérios de aceite
- Escopo da task atendido.
- Testes/build passam.
- Docker/local validado quando aplicável.
- Relatório final gerado.
- Commit local criado se tudo passar.
- Próxima task recomendada.

## Validação
- Rodar comandos relevantes definidos no LOCAL_RUNBOOK.
- Rodar testes do backend/frontend conforme arquivos alterados.
- Validar manualmente quando houver tela ou endpoint.

## Execução
Status: CONCLUIDA.

Resumo:
- Criada central de notificações internas no topo do shell.
- Adicionados eventos, avisos e alertas com contador de não lidas.
- Adicionados filtros `Todas` e `Não lidas`, ação de marcar notificação como lida e ação para marcar todas como lidas.
- Implementação inicial em estado local, sem persistência, fila, websocket ou push notification.

Validação executada:
- `corepack pnpm lint` em `web` com sucesso.
- `corepack pnpm typecheck` em `web` com sucesso.
- `corepack pnpm build` em `web` com sucesso apos pausar o dev server.
- `docker compose ps` com Postgres e Mailpit saudáveis.
- Backend local validado em `/actuator/health`.
- Web local `/app` retornou HTTP 200.
- Browser validou contador, painel, filtro de não lidas e marcar como lida sem erros no console.

Commit esperado:
- `feat(notificacoes): criar central de notificacoes internas (TASK-0905)`

## Prompt recomendado

```md
Execute TASK-0905 — Notificações internas seguindo o Harness Profissional.
Planeje internamente, implemente somente esta task, teste, valide Docker/local, revise, faça commit local se tudo passar e não faça push.
```
