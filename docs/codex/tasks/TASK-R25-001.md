# TASK-R25-001 — Contrato backend mobile/me

## Release

R25 — Mobile seguro por usuario autenticado.

## Objetivo

Criar endpoint dedicado para o app mobile descobrir o perfil autenticado, empresa ativa e vinculos de cliente/paciente do usuario sem expor entidade de persistencia.

## Escopo

- Endpoint `GET /api/mobile/me`.
- UseCase `ConsultarPerfilMobileUseCase`.
- Resolucao de usuario autenticado por JWT.
- Resolucao de empresa ativa.
- Resolucao de clientes vinculados por email dentro do tenant.

## Resultado

CONCLUIDA.

## Validacao esperada

- `mvn -q test`.
- Smoke test autenticado em `/api/mobile/me`.
