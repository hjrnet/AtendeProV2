databaseChangeLog:
  - changeSet:
      id: 0001-create-pgcrypto-extension
      author: atendepro
      dbms: postgresql
      changes:
        - sql:
            sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
      rollback:
        - sql:
            sql: DROP EXTENSION IF EXISTS pgcrypto;
